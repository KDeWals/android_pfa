<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="be.heh.pfa">

    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.VIBRATE" />

    <application
        android:allowBackup="false"
        android:icon="@mipmap/ic_launcher"
        android:installLocation="internalOnly"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.PFA">
        <activity android:name=".Activities.VueLiquideActivity"></activity>
        <activity android:name=".EditUserActivity" />
        <activity android:name=".Activities.VueComprimeActivity" />
        <activity android:name=".Activities.PLCViewActivity" />
        <activity android:name=".Activities.AddAutomateActivity" />
        <activity android:name=".Activities.AddUserActivity" />
        <activity android:name=".Activities.ManageAutomatesActivity" />
        <activity android:name=".Activities.ManageUsersActivity" />
        <activity
            android:name=".Activities.ProfileActivity"
            android:label="@string/title_activity_admin_profile"
            android:theme="@style/Theme.PFA.NoActionBar" />
        <activity
            android:name=".ProfileAdminActivity"
            android:label="@string/title_activity_profile_admin"
            android:theme="@style/Theme.PFA.NoActionBar" />
        <activity android:name=".Activities.HomeLambdaActivity" />
        <activity android:name=".Activities.HomeAdminActivity" />
        <activity android:name=".Activities.SignLambdaActivity" />
        <activity android:name=".Activities.CreateSuperUserActivity" />
        <activity android:name=".MainActivity" />
        <activity android:name=".Activities.LoginActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>